# Sonar

アイスブレークから自分の信念・大切にしたい感情まで降りていく対話アプリ。
AI は次の問いを1つ出すだけで、**結論を言うのは常に本人**。分岐の軌跡がマインドマップとして残る。

学校の2学期 個人開発の提出物⑦（ソースコード）。
Rust / Topcoat / Toasty（SQLite）/ Anthropic Claude Haiku 4.5。

## 最初に読むもの

**`docs/README.md`。** 全体像・読む順・ファイル一覧がそこにある。

手を動かす前に **`docs/next.md`** を必ず読む——いまの状態（§0）、次に何をどの順で（§2）、
起動と検査（§1）、**踏むと分かっている罠（§3）** が入っている。罠はすべて実測済みで、
読まずに始めると同じ時間を二度払うことになる。

## `docs/` は生成物。編集しない

`docs/` 配下は Obsidian Vault（Windows 側 `01_projects/06_sonar/`）からの**一方向コピー**。
正典は Vault にあり、生成は Vault 側の `python3 tools/build_docs.py` が行う。

- **`docs/` のファイルを直さない。** 直しても次の生成で消え、手編集はハッシュ照合で検出されて生成が止まる
- 実装中に気づいたこと（設計からの逸脱・踏んだ罠・下した判断）は `docs/` に書かず、
  **セッションの出力として報告する。** Vault 側の `詳細設計書` / `次の作業の入口` に反映され、
  再生成でここへ降りてくる
- `docs/README.md` の「生成日時」が古いと感じたら、Vault 側で再生成してから読む
- 根拠は `docs/adr/0005-dev-env.md` §3-3（提出物⑦の要件から別リポジトリ）と §3-4（`mock/` と同じ一方向コピー）

## 正典がどちらにあるか

| | 正典 | 備考 |
|---|---|---|
| 実装（Rust / `src/script.js` / `src/style.css`） | **このリポジトリ** | Vault の `mock/` は凍結済み。触らない |
| system プロンプト | **`src/prompt/system.md`** | Vault ではなくここ（`check/run.py` も実行時にこれを読む） |
| 設計の意図・決定の根拠・進行状態 | **Vault** | `docs/` にあるのは写し |

## 書くときの約束

- 決定の根拠を必ず明示する。「なぜそうしないか」も残す
- 迷ったら `docs/scope.md`（正典）→ `docs/adr/` の順に当たる。**ADR は蒸し返さないためのメモ**であり、
  そこで否決された案を再提案しない
- 困難に当たったときの選択肢は「やめる」ではなく「安く作り直す」。
  縮退の順序は `docs/scope.md` §6 が正典
